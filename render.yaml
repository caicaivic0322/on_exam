services:
  - type: static
    name: exam-platform
    buildCommand: npm ci && npm run build
    staticPublishPath: ./out
    envs:
      - key: NODE_VERSION
        value: "18"
      - key: NEXT_PRIVATE_TARGET
        value: "export"