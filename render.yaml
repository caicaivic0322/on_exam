services:
  - type: web
    name: exam-platform
    env: node
    plan: free
    buildCommand: npm ci && npm run build
    startCommand: npm run start
    envVars:
      - key: NODE_VERSION
        value: "18"
      - key: PORT
        value: "10000"
    healthCheckPath: /